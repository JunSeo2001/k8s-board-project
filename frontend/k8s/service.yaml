apiVersion: v1
kind: Service
metadata:
  name: cn-board-frontend-service
  labels:
    app: cn-board-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: cn-board-frontend

